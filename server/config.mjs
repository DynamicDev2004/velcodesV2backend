import {resolve,dirname} from 'node:path';import {fileURLToPath} from 'node:url';
const root=resolve(dirname(fileURLToPath(import.meta.url)),'..');
export function getConfig(){const production=process.env.NODE_ENV==='production',origin=process.env.APP_ORIGIN||'http://localhost:4180';if(production&&!origin.startsWith('https://'))throw new Error('Production APP_ORIGIN must use HTTPS.');return {production,origin:new URL(origin).origin,host:process.env.HOST||'127.0.0.1',port:Number(process.env.PORT||4180),databasePath:resolve(root,process.env.DATABASE_PATH||'data/velcodes.sqlite'),publicDir:resolve(root,'public'),siteDir:resolve(root,process.env.SITE_DIRECTORY||'../dist'),adminEmail:process.env.ADMIN_EMAIL||'info@velcodes.com',notificationEmail:process.env.NOTIFICATION_EMAIL||'info@velcodes.com',emailProvider:process.env.EMAIL_PROVIDER||'disabled',resendKey:process.env.RESEND_API_KEY||'',emailFrom:process.env.EMAIL_FROM||'Velcodes <portal@velcodes.com>',trustProxy:process.env.TRUST_PROXY==='true'}}
